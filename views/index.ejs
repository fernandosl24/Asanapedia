<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Welcome to Asanapedia</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet"> <!-- Google Fonts Link -->
    <style>
        body {
            font-family: 'Poppins', sans-serif; /* Apply Poppins globally */
            margin: 0;
            padding: 0;
            background-color: #f2f2f2;
            display: flex;
            flex-direction: column;
            justify-content: flex-start; /* Align content to the top */
            align-items: center;
            height: 100vh;
            overflow: hidden; /* Prevent scrolling */
        }
        .header {
            display: flex; /* Use flexbox for layout */
            justify-content: space-between; /* Adjusts content spacing */
            align-items: center; /* Center items vertically */
            width: 100%;
            padding: 10px 20px;
            background-color: #ffffff; /* Light background for the header */
            box-shadow: 0 2px 5px rgba(0,0,0,0.1); /* Subtle shadow for depth */
            text-align: left; /* Align text to the left */
            position: fixed; /* Keep the header at the top */
            top: 0;
            left: 0;
            z-index: 1000;
        }
        .header img {
            height: 30px; /* Adjust size as needed */
            margin-right: 10px; /* Space between logo and title */
        }
        .title {
            flex-grow: 1;
            text-align: center; /* Center the title text */
            font-weight: 600; /* Bolder font for the title */
        }
        .container {
            width: 100%;
            height: calc(100% - 50px); /* Adjust height accounting for header */
            margin-top: 50px; /* Push down the container by the header's height */
            overflow: hidden; /* Prevent scrolling */
        }
        iframe {
            width: 100%;
            height: 100%;
            border: none; /* Removes border around iframe */
        }
        h1 {
            color: #333;
            margin: 0;
            font-size: 24px; /* Larger font for better visibility */
        }
    </style>
</head>
<body>
    <div class="header">
        <a href="/">
            <img src="/img/greenyoga_logo.png" alt="GreenYoga Logo">
        </a>
        <h1 class="title">GreenYoga Asanapedia</h1>
        <div style="width: 30px;"> <!-- Spacer for balancing flexbox layout -->
        </div>
    </div>
    <div class="container">
        <iframe src="/documents/asanapedia.pdf#zoom=100" type="application/pdf"></iframe>
    </div>
</body>
</html>
